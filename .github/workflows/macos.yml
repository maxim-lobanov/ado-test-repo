name: Linux
on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    # runs-on: ubuntu-24.04-arm
    steps:
      - run: |
          printenv | sort
      - run: |
          ls -la /opt/
      - run: |
          rm -rf /opt/post-generation
          rm -rf /opt/post-generation
      - run: |
          ls -la /opt/
      - run: |
          cat /etc/passwd
